services:
  - type: web
    name: meribachat-curl-api
    env: node
    plan: free
    healthCheckPath: /api/health
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
