services:
  - type: web
    name: gift-card-balance-api
    # Using Docker environment directly since Playwright relies on OS dependencies
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
